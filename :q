export PATH=$PATH:/path/to/google-cloud-sdk/bin
export PATH=$PATH:/Users/ksheersagaragrawal/bin
# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/ksheersagaragrawal/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/ksheersagaragrawal/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/ksheersagaragrawal/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/ksheersagaragrawal/google-cloud-sdk/completion.zsh.inc'; fi
